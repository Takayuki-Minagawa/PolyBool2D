# 操作改善の実装記録

2026年9月19日。作業元: `../PolyBool2D_操作改善案.md`。

## 対応内容

| 改善案 | 実装 |
| --- | --- |
| 大きい図面の保存・切り替え | IndexedDBへの移行、図面・バックアップのトランザクション保存、旧データの移行、保存状態・時刻・例外の常時表示、再試行、JSON退避を確認して切り替え、読込先の保存失敗時の未保存オープン |
| A3横・縮尺・用紙 | A3/A4、縦横、縮尺、余白、左下原点、図形基準の配置、図枠の有無、ページごとのプレビュー、SVGのmmサイズ、複数図面のPDF出力 |
| 色と線幅 | 編集用最小線幅／実線幅切替、暗色背景での黒線補正、画面とSVG/PDFの線端・接合・破線・注記位置の共通化 |
| 大量要素の一覧 | 仮想スクロール、100要素以上は初期折りたたみ、レイヤー／グループ別、名前・種類検索、選択中／現在レイヤーの絞り込み、一覧と図形の相互移動 |
| PDF取込 | PDF.jsによるページ別のベクター抽出、回転、2点の矩形切り出し、2点の実寸校正、線・塗り・注記のレイヤー分離、警告表示 |
| 線整理 | 完全重複線の削除、分岐のない連続実線の連結、件数と対象の事前確認、Undo、ロック・拘束・グループの保護、破線属性の保存 |
| 読込直後の表示 | JSON／SVG／DXF／GeoJSON／PDF取込後の全体表示、用紙範囲に合わせる操作 |
| 書出の操作 | JSON/SVG/PDFの名称・形式・用紙・縮尺ダイアログ、対応ブラウザーの保存先選択、失敗・キャンセル・ページ進捗、ダウンロード開始とファイル保存完了の区別 |

IndexedDBは `polybool2d-projects` の `records` に図面、バックアップ、復旧原本、一覧と選択情報を保存します。図面本体は整形なしのJSONで保存して容量を削減し、外部JSON書出は読みやすい整形形式を維持します。既存のデータ検証と破損データの復旧ルールを引き継ぎます。タブ間の保存競合は更新番号で検出し、古いタブからの上書きを拒否します。

## 検証

- ローカルの型チェック、Vitest **68ファイル・483テスト**、production buildに成功。Playwrightの**3シナリオ**も成功。
- 一時Chromeプロファイルで、提供された実データ **4,786 / 6,903 / 32,222 要素**を順に読込。第3図の編集・保存・ページ再読込後も32,222要素を保持し、編集内容を復元。第1図への切り替えも確認。
- 同じ操作で、画面に生成される一覧のエンティティ行は最大8行。画面全体に全要素分の入力欄を作成しない。
- アプリから提供3図面を1つのPDFへ出力。Popplerで **3ページ、420 × 297 mm** を確認し、3ページすべてを画像化して描画を目視確認。
- **6000 mmの線 → 1/50 PDFの120 mm** を、PDFを再度ベクター読込して数値検証。mm/cm/m座標についても単体テスト。
- 配布ビルドを `/PolyBool2D/` パスで起動し、PDFワーカー・日本語フォント・PDF取込・IndexedDB保存を確認。
- 元PDFの全3ページをベクター取込し、JSONに戻しても破棄要素0。抽出要素数は6,170 / 39,744 / 44,274、第2ページでは220,431件の重複線を除去。元PDFの図枠等も対象なので、提供済みの整理済みJSONとは要素数が異なる。
- 旧データ移行失敗時の原本維持、IndexedDB書込失敗時のロールバック、再試行、削除後の遅延保存防止、タブ競合、線種と用紙設定の再読込を検証。

実ブラウザーテストは次で再実行できます。標準のPlaywrightブラウザーを用意済みなら `CHROME_PATH` は不要です。

```bash
CHROME_PATH='/Applications/Google Chrome.app/Contents/MacOS/Google Chrome' \
POLYBOOL_SAMPLE_DIR='/path/to/drawings' npm run test:e2e
```

## PDF取込・出力の範囲

PDF取込の文字は編集可能な注記に変換するため、字体・色・配置は近似です。文字輪郭の厳密な再現、画像の分離取込、任意のPDFクリップ形状、網掛け・パターンは未対応で、読込結果に警告します。切り出し境界をまたぐ塗りは除外します。曲線は16分割の折れ線です。

PDF出力では図形と既存の文字輪郭線をベクターのまま保持します。新規の日本語などの注記はブラウザーで描画した画像にして字体を保ちます。文字が印刷範囲をはみ出す判定は概算です。PDFの全ページには共通の用紙・縮尺・原点が適用されます。

保存先選択APIを利用できないブラウザーでは、アプリからダウンロードの実際の完了・保存フォルダーを検知できません。この場合は「ダウンロード開始」と表示し、ブラウザーのダウンロード一覧で確認する案内を出します。

## GitHub Actionsを消費しない配布準備

PR / push時のCIワークフローを削除し、Pagesワークフローを手動実行の配信専用へ変更しました。Actions上でnpm install・テスト・ビルドは行いません。

```bash
npm run prepare:pages
```

上記はローカルで型チェック・テスト・ビルドを実施し、`.pages-artifact/` にGitHub Project Pages用の成果物を作ります。commit・push・Actions起動はしません。

公開時は、成果物ディレクトリーの内容のみを別の `pages-artifact` ブランチにコミットしてpushし、そのコミットSHAを `deploy.yml` の `artifact_ref` に指定してください。Actionsはそのビルド済み成果物をアップロードしてPagesに登録する最後の処理だけに使います。
