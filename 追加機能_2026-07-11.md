# PolyBool2D 追加機能候補（2026-07-11 版）

> `main` ブランチ（ed7fade: アーキテクチャリファクタ後）のコードを再調査し、
> 有効な追加機能候補をまとめたものです。前回調査は [追加機能.md](追加機能.md)（2026-06-21）。
> 各候補は実装状況をコード上で検証済みです。

## 前回調査以降に実装済みとなった機能

以下は前回ロードマップの第1〜2階層で、現在の `main` に取り込み済みです。

- 計測拡張（周長・外接矩形・重心）: `geometry/measure.ts`
- 回転（90°）・拡縮（0.5×/2×）・左右上下ミラー: `geometry/transform2d.ts` + ストア `rotateSelected/scaleSelected/mirrorSelected`
- 凸包: `geometry/convexHull.ts`
- ポリゴン単純化（Douglas-Peucker）: `geometry/simplify.ts`
- 頂点の挿入・削除: `geometry/vertexEdit.ts` + `Alt+クリック` / パネル操作
- 整列・分布: `alignSelected` / `distributeSelected`
- 面積表示単位の切替（mm²/cm²/m²）: `app/units.ts` + 設定セレクタ
- SVG エクスポート: `persistence/svgExport.ts`
- CSV エクスポート（面積 / 頂点座標）: `persistence/csvExport.ts`
- ストア分割リファクタ: `app/store/`（entity / geometry / selection / history / ui に分離）

## 調査で確認した「土台はあるが UI が無い」機能

現在も次の資産が未配線のまま残っています。**低コスト・高価値**で着手できる筆頭候補です。

| 資産 | 定義場所 | 欠けているもの |
|------|----------|----------------|
| `Layer` 型・`project.layers`・`entity.layerId/visible/locked` | `app/projectTypes.ts:20-26` | レイヤー管理 UI 一式（components 内に Layer 参照ゼロ） |
| `PolygonGeometry.holes`（描画・面積・ドラッグ対応済み） | `geometry/types.ts` ほか | 穴を新規作成する UI・ストアアクション（`addHole` 相当が無い） |
| `GuideLineEntity` 型 | `app/projectTypes.ts:50-57` | ツール未登録（`ToolName` に無い）・描画・スナップ対象化 |
| `validatePolygon` / `ringHasSelfIntersection` | `geometry/validation.ts` | 作図・頂点編集パスから一切呼ばれていない |
| `rotateSelected(angleRad)` / `scaleSelected(sx, sy)`（任意値対応） | `app/store/types.ts:72-73` | UI は 90° / 0.5× / 2× 固定。任意角度・任意倍率の数値入力が無い |
| `settings.gridSize/circleSegments/precision` 等 | `app/projectTypes.ts:8-18` | `PropertyPanelSections` で読取表示のみ（編集可能なのは面積単位だけ） |
| `project.unit`（mm/cm/m） | `app/projectTypes.ts:65` | 読取表示のみで変更 UI が無い |

---

## 最優先（コスト対効果が特に高い）

| # | 機能 | 価値 | 難易度 | 根拠 |
|---|------|:----:|:------:|------|
| 1 | レイヤー管理パネル | 高 | 中 | モデル完備・UI 皆無。図面整理の根幹 |
| 2 | 穴（ホール）作図 UI | 高 | 中 | データ層完成済み。建築系で開口部は必須 |
| 3 | コピー & ペースト | 高 | 中 | `duplicateSelected` のみ。標準操作の欠落 |
| 4 | 任意角度回転・任意倍率拡縮の数値入力 | 高 | 低 | ストア API は既に任意値対応。UI 追加のみ |
| 5 | プロジェクト設定の編集 UI | 中〜高 | 低 | `updateSettings` 実装済み。フォーム追加のみ |
| 6 | 作図中の寸法 HUD（辺長・半径の実時間表示） | 高 | 中 | `ToolPreview.tsx` にラベル描画を追加 |

---

## 1. 作図・編集機能

### 1-1. 穴（ホール）の作図・編集 UI ★最優先
- **概要**: 既存ポリゴン内部に閉リングを描いて穴として追加。穴の削除・穴頂点の数値編集。
- **現状**: 穴のレンダリング・面積控除・頂点ドラッグは動作済み。穴を「作る」手段が difference 経由しかない。`pointInRing`・`ensureHoleCW` は既存。
- **実装方針**: ポリゴンツールに「穴モード」（選択中ポリゴン内で作図開始したら穴として取り込む等）を追加し、ストアに `addHole(entityId, ring)` を新設。内包判定は `pointInRing` を全頂点に適用。
- **難易度**: 中 / **価値**: 高

### 1-2. 任意角度回転・任意倍率拡縮の数値入力 ★最優先
- **概要**: プロパティパネルに角度（deg）・倍率の入力欄を設け、`rotateSelected` / `scaleSelected` を任意値で呼ぶ。
- **現状**: ストア API は任意値対応済み（`app/store/types.ts:72-73`）。UI が 90° / 0.5× / 2× ボタン固定。
- **難易度**: 低（UI のみ） / **価値**: 高 — 設計検討での使用頻度が高い。

### 1-3. 角度スナップ・直交（Ortho）拘束
- **概要**: 作図中に直前点からの角度を 15°/45°/90° にスナップ。`Shift` で水平垂直拘束。
- **現状**: 未実装。`snapping.ts` は座標系スナップのみで、作図文脈（直前点）を受け取らない。
- **実装方針**: `snapWorldPoint` に省略可能な `anchor: Point` を追加し、角度量子化を合成。
- **難易度**: 低〜中 / **価値**: 高 — 直線的な平面図の作図精度に直結。

### 1-4. 寸法・数値入力作図
- **概要**: 作図中に長さ・角度をキーボード入力して次点を確定（方向指定 + 「3000」入力）。
- **現状**: 未実装。`StatusBar` は座標表示のみ。
- **難易度**: 中〜高（インライン HUD 入力と方向×距離の合成） / **価値**: 高 — 寸法駆動は CAD の核。1-3 実装後だと方向指定が楽になる。

### 1-5. ガイドライン / 補助線ツール
- **概要**: 補助線を配置し、スナップ対象にする。
- **現状**: `GuideLineEntity` 型のみ存在。`ToolName`・`toolRegistry`・描画・スナップのすべてが未配線。
- **難易度**: 中 / **価値**: 中〜高（型が用意済みで実装意図が明確）。

### 1-6. 自己交差の検出フィードバック
- **概要**: ポリゴン確定時・頂点ドラッグ確定時に `validatePolygon` を呼び、警告表示（またはハイライト）する。
- **現状**: `geometry/validation.ts` は完備だが、`src/app` / `src/components` から呼び出しゼロ（grep で確認済み）。
- **難易度**: 低〜中 / **価値**: 中〜高 — 不正形状はブーリアン演算の失敗原因になるため早期警告の価値が大きい。

### 1-7. 楕円・円弧・ポリライン等の追加プリミティブ
- **現状**: 円のみ（`circle.ts`）。楕円は `circleToRing` の自明な拡張。
- **難易度**: 中 / **価値**: 中

---

## 2. UX・UI・操作性

### 2-1. レイヤー管理パネル ★最優先
- **概要**: レイヤーの一覧・追加/削除/リネーム・表示 ON/OFF・ロック・色設定、エンティティのレイヤー割当。
- **現状**: `Layer` 型・`project.layers`・`entity.layerId/visible/locked` は定義済みだが、components 配下に Layer 参照が一切ない（grep で確認済み）。
- **実装方針**: ストアにレイヤー CRUD アクションを追加し、サイドパネルを新設。`CadViewport` の描画・ヒットテストに `visible`/`locked` の分岐を追加。
- **難易度**: 中 / **価値**: 高

### 2-2. コピー & ペースト（Ctrl/⌘+C/V/X）★最優先
- **概要**: 選択エンティティのコピー・貼付・切取。
- **現状**: 未実装。`duplicateSelected`（固定オフセット複製）のみ。clipboard 系の実装ゼロ（grep で確認済み）。
- **留意**: `C` キーは円ツールに割当済み。`Ctrl/⌘` 修飾付きのみ clipboard に振る分岐が必要（既存の `Ctrl+A` 処理と同様のパターン）。
- **難易度**: 中 / **価値**: 高

### 2-3. プロジェクト設定の編集 UI ★最優先
- **概要**: グリッドサイズ・円分割数・スナップ種別/許容値・表示精度・座標単位（mm/cm/m）を GUI から編集。
- **現状**: `updateSettings` はストアに実装済み（redo 破損バグも修正済み）。UI は面積表示単位セレクタ以外すべて読取表示（`PropertyPanelSections.tsx:220-225` 等で確認済み）。
- **難易度**: 低 / **価値**: 中〜高 — 実装コストが最も低い部類。

### 2-4. コンテキストメニュー（右クリック）
- **概要**: 複製・削除・ブーリアン・レイヤー移動などを右クリックから実行。
- **現状**: 未実装（`onContextMenu` 使用ゼロ）。アクションは既存ストア関数を再利用可能。
- **難易度**: 中 / **価値**: 高 — 操作距離を大幅短縮。

### 2-5. 作図中の寸法 HUD ★最優先
- **概要**: 作図プレビュー上に辺長・矩形の W×H・円半径をリアルタイム表示。カーソル座標のビューポート内表示。
- **現状**: `ToolPreview.tsx` は形状のみ描画しラベル無し（確認済み）。座標はステータスバーのみ。
- **実装方針**: プレビュー SVG に `<text>` を追加。`measure.ts` の関数を流用。
- **難易度**: 低〜中 / **価値**: 高 — 寸法を見ながら作図できるのは CAD の本質。

### 2-6. 測定ツール（距離 / 角度）
- **概要**: 形状を生成しない一時的な 2 点間距離・3 点角度・累積長の計測ツール。
- **現状**: 未実装。`toolRegistry` に measure 系ツールなし。`numeric.ts` の距離関数を流用可能。
- **難易度**: 中 / **価値**: 高

### 2-7. ショートカット一覧（チートシート）
- **概要**: `?` キーで全ショートカットのモーダル表示。
- **現状**: 未実装。`TOOL_DEFINITIONS` にキー情報が集約されたため、ツール系は registry から自動生成できる（リファクタで実装コストが下がった）。
- **難易度**: 低 / **価値**: 中〜高 — 発見性・オンボーディングに直結。

### 2-8. エンティティ一覧 / アウトライナー
- **概要**: 全エンティティのリスト表示・クリック選択・表示/ロック切替・リネーム。2-1 のレイヤーパネルと統合実装を推奨。
- **難易度**: 中 / **価値**: 中〜高

---

## 3. 入出力・データ管理

### 3-1. 複数プロジェクト管理
- **概要**: localStorage に複数プロジェクトを保存し、一覧から開く/複製/削除/リネーム。
- **現状**: `localProjectStore.ts` が単一キー `pb2d.project` 固定（確認済み）。`project.id`/`name` は存在するが未活用。
- **実装方針**: `pb2d.project.<id>` + インデックスキーに変更し、一覧モーダルを追加。旧キーからのマイグレーションを 1 回だけ実行。
- **難易度**: 中 / **価値**: 高 — 複数案件対応と誤上書き防止。

### 3-2. 自動保存の世代管理 / バックアップ
- **概要**: 自動保存時に直近 N 世代のスナップショットを保持し復元できるようにする。
- **現状**: 未実装（毎回上書き）。README にも「localStorage はバックアップではない」と明記されたまま。
- **難易度**: 中 / **価値**: 中〜高 — データ喪失リスクへの直接的な手当て。

### 3-3. PNG（ラスター）書き出し
- **概要**: ビューポートを PNG 保存。レビュー共有・資料貼付用。
- **現状**: 未実装。`svgExport.ts` が既にあるため SVG→canvas→`toBlob` で実現可能。CSS 変数の実値解決は svgExport と共通化。
- **難易度**: 低〜中 / **価値**: 中〜高

### 3-4. DXF エクスポート
- **概要**: ポリゴンを DXF（LWPOLYLINE、R12 ASCII サブセット）として出力。AutoCAD / Jw_cad 連携。
- **現状**: 未実装（README の既知の制限に明記）。リング配列を素直にマップ可能で外部ライブラリ不要。
- **難易度**: 中 / **価値**: 高 — 建築・構造ユーザーに最重要の相互運用フォーマット。

### 3-5. SVG インポート
- **概要**: SVG の `<path>`/`<polygon>`/`<rect>`/`<circle>` を読み込みポリゴン化。
- **現状**: 未実装。インポートは JSON のみ。`DOMParser` + `getPointAtLength` で外部依存なしに実装可能。
- **難易度**: 中〜高（曲線近似を含めると高） / **価値**: 高

### 3-6. 共有用 URL エンコード
- **概要**: プロジェクト JSON を圧縮 + base64url 化して URL ハッシュで共有。
- **難易度**: 中（URL 長制限のガード必要） / **価値**: 中

---

## 4. ジオメトリ演算の拡充

### 4-1. オフセット / バッファ
- **概要**: 輪郭を指定距離だけ内外にオフセットした新ポリゴンを生成。壁厚・クリアランス検討用。
- **現状**: 未実装。「各辺を法線方向へ平行移動 → union で自己交差除去」の簡易実装なら既存の `polygonClippingEngine` で完結。
- **難易度**: 中〜高（簡易版は中） / **価値**: 高 — 2D CAD の中核機能で、未実装のジオメトリ演算では最有力。

### 4-2. 自己交差の自動修復
- **概要**: 1-6 の検出に加え、`union(self)` によるリング正規化で自己交差を解消するボタン。
- **現状**: 検出関数のみ存在・未配線。修復は `polygonClipping.union([self])` で大半対応可能。
- **難易度**: 中 / **価値**: 中〜高

### 4-3. フィレット / 面取り
- **概要**: 指定頂点を半径 R の円弧近似（フィレット）/ 直線カット（面取り）で処理。
- **現状**: 未実装。円弧は既存方針どおり多角形近似で保存すれば型変更不要。
- **難易度**: 中 / **価値**: 中

### 4-4. ナイフ分割の拡張
- **概要**: 「直線 1 本・2 交点・穴なし」制約の緩和（複数交点・穴あき対応）。
- **現状**: `knifeSplit.ts` が `has-holes` / `not-two-intersections` を明示拒否。
- **代替実装**: ナイフ線を細幅ポリゴン化して difference する近似なら難易度中。
- **難易度**: 高（本実装）/ 中（近似） / **価値**: 中

### 4-5. 最小外接矩形（rotating calipers）
- **概要**: 材料取り・配置検討向け。凸包（実装済み）を前提にできるため着手コストが下がった。
- **難易度**: 中 / **価値**: 中

### 4-6. 空間インデックス
- **概要**: スナップ・ヒットテスト・自己交差検出の高速化（現状は線形走査・O(n²)）。
- **難易度**: 中 / **価値**: 中（エンティティ数が増えた段階で効く。機能追加より後回しで可）。

---

## 推奨ロードマップ（コスト対効果順）

1. **設定編集 UI**（2-3）／**任意角度・倍率の数値入力**（1-2）／**寸法 HUD**（2-5）／**ショートカット一覧**（2-7）
   → いずれもストア・ジオメトリ層が完成済みで、UI 追加だけで完結する低コスト帯。
2. **レイヤー管理**（2-1）／**穴 UI**（1-1）／**コピペ**（2-2）／**自己交差フィードバック**（1-6）
   → モデル・検証関数は揃っており、配線すれば体験が大きく向上する。
3. **角度スナップ**（1-3）／**測定ツール**（2-6）／**コンテキストメニュー**（2-4）
   → 作図精度と操作効率の底上げ。
4. **複数プロジェクト管理**（3-1）／**バックアップ世代**（3-2）
   → 永続化基盤の強化とデータ喪失対策。
5. **DXF エクスポート**（3-4）／**SVG インポート**（3-5）／**PNG 書き出し**（3-3）／**オフセット**（4-1）
   → 相互運用と CAD コア演算の拡充。中コスト・高価値。

## 横断的な技術メモ

- 前回指摘の `UNIT_FACTOR` 重複は `app/units.ts` への集約で**解消済み**。
- ストアが `app/store/` に責務分割されたため、レイヤー CRUD 等の新アクションは `entityActions.ts` / `uiActions.ts` のパターンに沿って追加すればよい。
- `TOOL_DEFINITIONS`（`toolRegistry.ts`）にツールとショートカットが集約されたため、新ツール追加（ガイドライン・測定）とチートシート自動生成の両方が容易になった。
- SVG/PNG 書き出しで必要な「CSS 変数の実値解決」は `svgExport.ts` に前例があるはずなので、PNG 実装時はそこと共通化する。
- `SUPPORTED_VERSIONS` は現行バージョン厳格一致のまま。設定項目・エンティティ型を増やす（レイヤー活用・ガイドライン等）際は、旧版 JSON のマイグレーション層を先に用意することを推奨。
