# PolyBool2D 追加機能候補

> 4つの観点（作図・編集 / UX・UI / 入出力・相互運用 / ジオメトリ演算）について
> 複数エージェントでコードベースを調査し、機能追加候補をまとめたものです。
> 調査日: 2026-06-21 / 対象: `main` ブランチ

## ✅ 本ブランチ (`feat/feature-expansion`) で実装済み

ロードマップの最優先〜第2階層を中心に、以下を実装しました（すべて単体テスト + typecheck + build 通過）。

| 項目 | 対応セクション | 実装場所 |
|------|----------------|----------|
| 計測拡張（周長・外接矩形・重心） | 4-1 / 5 | `geometry/measure.ts`, PropertyPanel |
| 回転・拡縮・ミラー | 1-2 | `geometry/transform2d.ts`, ストア `rotate/scale/mirrorSelected` |
| 凸包 | 4-3 | `geometry/convexHull.ts`, ストア `convexHullSelected` |
| ポリゴン単純化 (Douglas-Peucker) | 4-4 | `geometry/simplify.ts`, ストア `simplifySelected` |
| 頂点の挿入・削除 | 1-1 | `geometry/vertexEdit.ts`, パネル + `Alt+クリック` |
| 整列・分布 | 2-8 / 4列目 | ストア `alignSelected/distributeSelected` |
| 単位ユーティリティ集約 + 面積単位選択 | 3-2 | `app/units.ts`, 設定セレクタ |
| SVG エクスポート | 3-1 | `persistence/svgExport.ts` |
| CSV エクスポート（面積 / 頂点） | 3-3 | `persistence/csvExport.ts` |

未実装の候補（レイヤー管理、コンテキストメニュー、コピペ、DXF、オフセット 等）は引き続き下記ロードマップを参照。

---

## 調査で分かった重要な前提

現状のコードベースには「**データモデルや純粋関数の土台はあるが、UI が繋がっていない**」未完成機能が多く存在します。これらは低コスト・高価値で着手できます。

- `Layer` 型・`project.layers`・`entity.layerId`/`visible`/`locked` は定義済みだが**レイヤー管理 UI が皆無**。
- `PolygonGeometry.holes`（穴）はレンダリング・面積計算・頂点ドラッグまで対応済みだが**穴を作る UI がない**。
- `GuideLineEntity` 型は定義済みだが**作図ツール・描画・スナップが未配線**。
- `ringHasSelfIntersection`/`validatePolygon`（自己交差検出）は完備だが**作図・編集パスで呼ばれていない**。
- `ringCentroid`（重心計算）は実装済みだが**UI で未使用**。
- `unit`（mm/cm/m）はモデルにあるが**UI は読み取り専用**、面積は m² 固定。
- **Intersection / XOR 演算は既に実装済み**（union/difference と同様に呼び出し可能）。

---

## 最優先（コスト対効果が特に高い）

既存の純粋関数・データモデルが揃っており、主に UI 配線で実現できる高価値機能。

| # | 機能 | 価値 | 難易度 | 状況 |
|---|------|:----:|:------:|------|
| 1 | 頂点の挿入・追加 / 単一頂点の削除 | 高 | 低〜中 | 未実装（README に制限明記） |
| 2 | ポリゴンの回転・スケール・ミラー | 高 | 中 | 未実装（`translate.ts` は平行移動のみ） |
| 3 | SVG エクスポート | 高 | 低 | 未実装（描画は既に SVG） |
| 4 | 単位設定 UI + 面積単位選択 | 高 | 低〜中 | 部分実装（`unit` はあるが UI 読取専用） |
| 5 | 計測拡張（周長・外接矩形・重心の表示） | 高 | 低 | 部分実装（面積のみ表示、`ringCentroid` 未使用） |

---

## 1. 作図・編集機能

### 1-1. 頂点の挿入・追加と単一頂点の削除 ★最優先
- **概要**: 辺上クリックで頂点を挿入し、Alt+クリック / Delete / 右クリックで単一頂点を削除する。
- **現状**: 未実装（README「既知の制限」に明記）。挿入は既存 `nearestEdgePoint`（`snap.ts`）を流用可。`normalizeRing` が 3 頂点未満を既に弾く。
- **難易度**: 低〜中 — データ操作はリング配列の splice のみ。UI のディスパッチ配線が中心。
- **価値**: 高 — 最も基本的な CAD 編集機能で、現状の最大の欠落。
- **関連**: `VertexHandles.tsx`, `CadViewport.tsx`, `PropertyPanel.tsx`

### 1-2. 回転・スケール・ミラー（変形）★最優先
- **概要**: 選択形状を基準点（重心 / 任意点）まわりに回転・拡縮・水平/垂直ミラー。数値入力も併設。
- **現状**: 未実装。`translate.ts` に平行移動のみ。`ringCentroid`・`boundsForEntities` を基準点に流用可。
- **難易度**: 中 — 純粋関数（rotate/scale/mirror）は容易。変形ハンドル UI とプレビューに手間。回転後の非整数座標と `normalize` の 9 桁丸めの整合確認が必要。
- **価値**: 高 — 設計検討で頻出。現状は数値編集で 1 点ずつしか動かせない。
- **関連**: `translate.ts`（新規関数）, `transform.ts`, `appStore.ts`, `VertexHandles.tsx`

### 1-3. 穴（ホール）の作図・編集 UI
- **概要**: 既存ポリゴン内部にリングを描いて穴として追加 / 削除 / 穴頂点の数値編集。
- **現状**: 部分実装。`holes` のレンダリング・面積・ドラッグは対応済みだが**穴の新規作成 UI がない**。`pointInRing`・`ensureHoleCW` は既存。
- **難易度**: 中 — 「内部に閉ポリゴンを描く→親に取り込む」フローと内包判定が必要。データ層はほぼ完成。
- **価値**: 高 — 開口部のある平面領域は建築・構造で必須。difference 経由は手数が多い。

### 1-4. 角度スナップ・直交（Ortho）拘束
- **概要**: 作図中に直前点からの角度を 15°/45°/90° 等にスナップ、Shift で水平垂直拘束。
- **現状**: 未実装。スナップは座標系（グリッド/頂点/辺）のみ。`snapWorldPoint`（`snapping.ts`）が拡張点。
- **難易度**: 低〜中 — 直前点を作図文脈から渡す配線が要る。角度量子化計算は単純。
- **価値**: 高 — 直線的な平面図を正確に描く基本。

### 1-5. 寸法・数値入力作図（長さ・角度をキー入力）
- **概要**: 作図中に長さ・角度をキーボード入力して確定（方向指定 + 「3000」入力）。
- **現状**: 未実装。`StatusBar` は座標表示のみで入力不可。
- **難易度**: 中〜高 — 数値入力中のインライン HUD と方向ベクトル×距離の合成が必要。
- **価値**: 高 — 寸法駆動は CAD の核。マウスのみでは精密設計が困難。

### 1-6. ガイドライン / 補助線ツール
- **概要**: 補助線を配置し、それにスナップして作図する。
- **現状**: 部分実装。`GuideLineEntity` 型は定義済みだが**ツール・描画・スナップ対象化が未実装**、`ToolName` 未登録。
- **難易度**: 中 — 型の土台はあり、ツール追加・SVG 描画・スナップ統合が必要。
- **価値**: 中〜高（型が用意済みで実装意図が見える）。

### 1-7. 楕円・円弧・折れ線（ポリライン）プリミティブ
- **概要**: 楕円・円弧・開いた折れ線の作図ツール追加。
- **現状**: 未実装（円のみ `circle.ts`）。楕円は `circleToRing` の自明な拡張。
- **難易度**: 中。
- **価値**: 中。

### 1-8. パラメトリック形状（円・矩形のパラメータ保持）
- **概要**: 円の中心・半径、矩形の寸法を保持し、後から数値編集・分割数変更を可能に。
- **現状**: 未実装。生成時に多角形化して捨てている（`metadata.sourceShape` のタグのみ）。
- **難易度**: 中〜高 — エンティティ型・シリアライザ・検証・再生成パイプラインの拡張。
- **価値**: 中。

### 1-9. 自己交差の検出フィードバックと作図ガード
- **概要**: 作図 / 頂点ドラッグ中に自己交差を検知し警告表示・確定防止。
- **現状**: 部分実装。`validation.ts` に検出ロジック完備だが**作図・編集パスで未使用**。
- **難易度**: 中 — 検出関数を確定時に呼び `errorMessage`/ハイライトに繋ぐ。
- **価値**: 中。

---

## 2. UX・UI・操作性

### 2-1. レイヤー管理パネル
- **概要**: レイヤーの一覧・追加/削除/リネーム、表示 ON/OFF、ロック、色設定。エンティティのレイヤー割り当て。
- **現状**: 部分実装。`Layer` 型・`project.layers`・`entity.layerId`・`visible/locked/color` は定義済みだが**UI 皆無**（`PropertyPanel` で読取表示のみ）。
- **難易度**: 中 — ストアアクション追加とパネル構築が中心。`CadViewport` の描画・ヒットテストが `visible`/`locked` を尊重する分岐が必要。
- **価値**: 高 — 図面整理の根幹機能。

### 2-2. コンテキストメニュー（右クリック）
- **概要**: ビューポート / エンティティ上で右クリックし、複製・削除・ブーリアン・前面背面・レイヤー移動を呼ぶ。
- **現状**: 未実装（`onContextMenu` なし）。アクション自体は既存ストア関数を再利用可。
- **難易度**: 中。
- **価値**: 高 — 操作距離を大幅短縮。

### 2-3. コピー & ペースト（クリップボード）
- **概要**: `Ctrl/⌘+C/V/X` で選択エンティティをコピー・貼付・切取。
- **現状**: 未実装。`duplicateSelected`（固定オフセット複製）のみ。**`C` キーは円ツールに割当済みで競合整理が必要**。
- **難易度**: 中 — 内部クリップボード state と ID 再生成・位置決め。
- **価値**: 高 — ユーザー期待値が非常に高い標準操作。

### 2-4. 測定ツール（距離 / 角度）
- **概要**: 2 点間距離・3 点角度・累積長を計測する専用ツール（形状を生成しない一時線）。
- **現状**: 未実装。`numeric.ts` の距離関数を流用可。
- **難易度**: 中 — 新ツールモード・一時描画・結果表示。
- **価値**: 高 — 面積に加え寸法確認のニーズは大きい。

### 2-5. 座標・寸法の常時表示（HUD オーバーレイ）
- **概要**: ビューポート上にカーソル座標を常時表示。作図中は辺長・矩形寸法・円半径をリアルタイム表示。
- **現状**: 部分実装。座標はステータスバーのみ、プレビューに寸法ラベルなし。
- **難易度**: 中。
- **価値**: 高 — 寸法を見ながら作図できるのは CAD の本質。

### 2-6. ショートカット一覧表示 + カスタマイズ
- **概要**: 全ショートカットのチートシート（`?` キー）。将来はリバインド対応。
- **現状**: 未実装。ショートカットは `App.tsx`/`CadViewport.tsx` にハードコード。
- **難易度**: 低（一覧）/ 中（カスタマイズ）。
- **価値**: 中〜高 — 発見性・オンボーディングに直結。

### 2-7. エンティティ一覧 / アウトライナーパネル
- **概要**: 全エンティティをリスト表示、クリックで選択、表示/ロック/リネーム/重なり順操作。レイヤーパネルと統合可。
- **現状**: 未実装（`PropertyPanel` は選択中のみ表示）。`name`/`visible`/`locked` フィールドは既存。
- **難易度**: 中。
- **価値**: 中〜高 — 図形が増えると重なり選択が困難になるため有用。

### 2-8. 整列・分布（Align / Distribute）
- **概要**: 複数選択を左/中/右/上/下揃え、等間隔分布するボタン群。
- **現状**: 未実装。複数選択・`translateEntities`・`boundsForEntities` の部品は揃う。
- **難易度**: 低〜中 — バウンディングボックスから目標オフセット算出 → `translateEntities`。
- **価値**: 中。

### 2-9. プロジェクト設定の編集 UI
- **概要**: グリッドサイズ・円分割数・精度・スナップ種別・許容値を GUI から編集（現状コード/JSON 経由）。
- **現状**: 未実装。`settings.*` はモデルにあるが `SettingsSection` は読取専用。
- **難易度**: 低〜中。
- **価値**: 中。

### 2-10. その他の小改善
- **ルーラー（定規）表示**: 難易度中 / 価値中。
- **テーマ 3 状態化（light/dark/auto）**: `readTheme` はシステム参照済み。難易度低 / 価値中。
- **取り消し履歴 UI（History パネル）**: `history.past/future` は既存。難易度中 / 価値中。
- **ミニマップ**: 難易度中 / 価値低〜中（Fit + パンで概ね代替可）。
- **トランスフォームギズモ**: 1-2 と統合実装が望ましい。難易度高 / 価値中〜高。
- **グループ化**: モデル拡張が広範。難易度高 / 価値中（複数選択で代替可）。
- **スマートガイド / 整列ガイド**: 難易度高 / 価値中（既存スナップで多くをカバー）。
- **注釈・ラベル・テキスト**: 新エンティティ型が必要。難易度高 / 価値中〜高。

---

## 3. 入出力・相互運用・データ管理

### 3-1. SVG エクスポート ★最優先
- **概要**: 図面（全体 / 選択）を SVG ファイルとして書き出す。Illustrator/Inkscape/他 CAD への受け渡し。
- **現状**: 未実装。だが描画は既に SVG で、ポリゴンは `<path>`（outer + holes / fill-rule evenodd）に素直にマップ可。
- **難易度**: 低 — 座標→path 文字列生成 + Blob ダウンロード（`exportProjectFile` と同パターン）。外部依存不要。
- **価値**: 高 — ベクター相互運用の最短経路。PNG/PDF/クリップボードの共通基盤。

### 3-2. 単位設定 UI + 面積単位選択 ★最優先
- **概要**: 単位（mm/cm/m、できれば inch）を UI で変更し、面積表示単位（m²/cm²/mm²）も選べるように。
- **現状**: 部分実装。`unit` と `UNIT_FACTOR` は存在するが UI は読取専用、面積は m² ハードコード。
- **難易度**: 低〜中 — セレクタ追加と `UNIT_FACTOR` の一元化。
- **価値**: 高 — 面積確認が主目的のツールで単位を選べないのは実用上の制約。
- **留意**: `UNIT_FACTOR` が `StatusBar.tsx` と `PropertyPanel.tsx` に**重複定義**。着手前に `src/app/units.ts` 等へ共通化推奨。

### 3-3. CSV 出力（座標一覧 / 面積一覧）
- **概要**: 全/選択ポリゴンの面積一覧・頂点座標一覧を CSV 出力。Excel での集計・検算用。
- **現状**: 未実装。`polygonArea`・`signedRingArea` は既存。
- **難易度**: 低 — エンティティ走査 → 文字列組立 → Blob。
- **価値**: 中〜高 — 「面積確認」という主目的に直結。

### 3-4. 複数プロジェクト管理 / プロジェクト一覧
- **概要**: localStorage に複数プロジェクトを保存し、一覧から開く/複製/削除/リネーム。
- **現状**: 部分実装。`localProjectStore` が単一キー `pb2d.project` 固定。`id`/`name` は既存だが未活用。
- **難易度**: 中 — キー設計を `pb2d.project.<id>` + インデックスに変更し一覧 UI 追加。
- **価値**: 高 — 複数案件を扱う基盤。誤上書き事故も防止。

### 3-5. PNG（ラスター）書き出し
- **概要**: ビューポートを PNG として保存。レビュー共有・資料貼付。
- **現状**: 未実装。SVG→canvas→`toBlob` で実現可。
- **難易度**: 低〜中 — CSS 変数（`var(--cad-fill)` 等）を実値に解決する処理が必要（3-1 と共通化推奨）。
- **価値**: 中〜高 — 最も手軽な成果物共有手段。

### 3-6. SVG インポート（パス→ポリゴン）
- **概要**: SVG の `<path>`/`<polygon>`/`<rect>`/`<circle>` を読み込みポリゴン化。下絵や既存図面の取り込み。
- **現状**: 未実装。`addPolygonFromOuter`（正規化込みの取り込み口）は既存。
- **難易度**: 中〜高 — 直線中心の path なら中、曲線近似まで含めると高。`DOMParser` + `getPointAtLength` で外部依存不要。
- **価値**: 高 — インポートが現状 JSON のみ。用途が大きく広がる。

### 3-7. DXF エクスポート
- **概要**: ポリゴンを DXF（LWPOLYLINE）として出力。AutoCAD/Jw_cad 等との連携。
- **現状**: 未実装（README に制限明記）。リング配列を素直にマップ可。
- **難易度**: 中 — R12 ASCII サブセットなら外部ライブラリ不要。穴の扱い・単位（$INSUNITS）で手間。DWG は非対応で割り切る。
- **価値**: 高 — 対象ユーザー（建築・構造）に最重要の相互運用フォーマット。

### 3-8. 自動保存の世代管理 / バックアップ
- **概要**: 自動保存時に直近 N 世代のスナップショットを保持し復元可能に。
- **現状**: 未実装（毎回上書き）。README に「localStorage はバックアップではない」と明記。
- **難易度**: 中 — リングバッファ的に保存 + 復元 UI。`history`(undo) とは別概念で整理。
- **価値**: 中〜高 — データ喪失リスクへの直接的手当て。

### 3-9. その他の入出力候補
- **面積集計レポート（PDF/印刷）**: `window.print()` + `@media print` なら中、jsPDF は高。価値中〜高（3-1/3-5/3-3 と重複あり）。
- **共有用 URL エンコード**: JSON→圧縮→base64url→hash。URL 長制限のガード必要。難易度中 / 価値中。
- **下絵画像読み込み + 手動トレース**: 画像の永続化方針（dataURL の肥大化）が課題。難易度中〜高 / 価値中。
- **クリップボード経由 SVG/PNG コピー**: 3-1 流用可。難易度中 / 価値中。
- **テンプレート / 部品ライブラリ**: 3-4 と保存基盤を共有。難易度中 / 価値中。
- **DXF インポート**: 仕様が広く外部ライブラリ必須。難易度高 / 価値中〜高（エクスポート先行を推奨）。

---

## 4. ジオメトリ演算の拡充

### 4-1. 計測拡張（周長・外接矩形・重心）★最優先
- **概要**: 周長・外接矩形(W×H)・重心座標・最小外接矩形を `PropertyPanel` に表示。
- **現状**: 部分実装。面積のみ表示。`ringCentroid` は実装済みだが**未使用**。周長・bbox 関数は要追加（`boundsForEntities` は流用可）。
- **難易度**: 低 — 周長は辺長総和、bbox は既存流用、重心は既存関数呼出。
- **価値**: 高 — 既存資産で安価に実装でき、CAD 利用者が常時欲しい情報。

### 4-2. オフセット / バッファ（内外への膨張・収縮）
- **概要**: 輪郭を指定距離オフセットした新ポリゴンを生成。壁厚・クリアランス用途。
- **現状**: 未実装。
- **難易度**: 中〜高 — 自前実装は自己交差処理が必要。「各辺を法線移動 → union/difference で自己交差除去」の簡易版なら中。
- **価値**: 高 — 2D CAD の中核機能。

### 4-3. 凸包（Convex Hull）
- **概要**: 点群 / 複数ポリゴンの頂点集合から凸包を生成。
- **現状**: 未実装。
- **難易度**: 低 — Andrew's monotone chain は数十行。`numeric.ts` の `cross` 再利用。
- **価値**: 中〜高 — 包絡形状・干渉判定の前処理。

### 4-4. ポリゴン単純化（Douglas-Peucker）
- **概要**: 許容誤差内で頂点を間引く。円(64分割)や boolean 結果の冗長頂点を削減。
- **現状**: 未実装（`normalize` は隣接重複除去のみで共線点は残る）。
- **難易度**: 低〜中。
- **価値**: 中〜高 — 頂点爆発に直接効き、性能・可読性が向上。

### 4-5. 自己交差の検出強化と自動修復
- **概要**: 検出に加え `union(self)` でリング正規化し自己交差を解消。穴の包含/重なりチェックも追加。
- **現状**: 部分実装。検出のみ。穴チェック（`hole-outside-outer`/`hole-overlap`）は型にあるが未実装。
- **難易度**: 中 — 修復は `polygonClipping.union([self])` で大半対応。
- **価値**: 中〜高 — 頂点編集で壊れる不正形状の救済。

### 4-6. フィレット / 面取り
- **概要**: 指定頂点を半径 R の円弧（フィレット）/ 直線カット（面取り）で処理。
- **現状**: 未実装。
- **難易度**: 中 — 鋭角・短辺の境界条件処理に注意。
- **価値**: 中。

### 4-7. ナイフ分割の拡張
- **概要**: 「直線1本・2交点・穴なし」制約を撤廃し、折れ線 / 複数交点 / 穴あきに対応。
- **現状**: 部分実装。`knifeSplit.ts` が `has-holes`/`not-two-intersections` を明示的に拒否。
- **難易度**: 高 — 平面細分の本実装か、ナイフの細幅ポリゴン化 + difference 近似。
- **価値**: 中。

### 4-8. その他のジオメトリ候補
- **距離 / 最近接点 API**: 計測ツール（2-4）の基盤。難易度中 / 価値中。
- **空間インデックス（R-tree/グリッド）**: スナップ・交差の高速化。現状は線形走査・自己交差 O(n²)。難易度中 / 価値中（規模拡大時に効く）。
- **最小外接矩形（rotating calipers）**: 凸包(4-3)前提。材料取り向け。難易度中 / 価値中。
- **ポリゴンの隣接検出 / 結合判定**: 共有エッジ検出。難易度中 / 価値低〜中。
- **慣性モーメント / 断面二次モーメント**: 構造計算向け。難易度中 / 価値低。
- **robust predicates（Shewchuk）導入**: 縮退ケース安定化。難易度高 / 価値低（現状で十分安定なら ROI 低）。

---

## 推奨ロードマップ（コスト対効果順）

1. **計測拡張**（4-1）／**SVG エクスポート**（3-1）／**単位 UI**（3-2）／**CSV 出力**（3-3）
   → いずれも低コスト・高価値。既存資産で完結し、主目的「面積確認」に直結。
2. **頂点挿入・削除**（1-1）／**回転・スケール・ミラー**（1-2）／**角度スナップ**（1-4）
   → 基本的な CAD 編集の欠落を埋める。土台あり、UI 配線が中心。
3. **レイヤー管理**（2-1）／**コンテキストメニュー**（2-2）／**コピペ**（2-3）／**穴 UI**（1-3）
   → モデルは揃っており UI を繋ぐだけで体験が大きく向上。
4. **複数プロジェクト管理**（3-4）／**バックアップ**（3-8）／**測定ツール**（2-4）
   → 永続化基盤の底上げとデータ喪失対策。
5. **DXF エクスポート**（3-7）／**SVG インポート**（3-6）／**オフセット**（4-2）
   → 相互運用と CAD コア演算の拡充。中コスト・高価値。

## 横断的な技術メモ

- `UNIT_FACTOR` が `StatusBar.tsx` と `PropertyPanel.tsx` に**重複定義**。単位機能着手前に共通 util 化（`src/app/units.ts`）を推奨。
- スタイルに CSS 変数（`var(--cad-fill)` 等）を使用。SVG/PNG 書き出し時は computed style への解決が共通で必要。
- `SUPPORTED_VERSIONS` が現行バージョン厳格一致。インポート系を増やす際は旧版マイグレーション層の検討を推奨。
- 円・矩形は多角形展開済みで元パラメータ非保持。DXF で真円・矩形として出すには `metadata` 拡張が別途必要（1-8 と関連）。
- 多くのジオメトリ候補（4-1/4-3/4-4）は `numeric.ts`・`normalize.ts` の再利用で**新規ライブラリ依存なし**で実装可能。
