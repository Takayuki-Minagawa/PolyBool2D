# PolyBool2D 作業計画 — リファクタリング & 機能追加

> 作成日: 2026-07-29 / 対象: `main` (b873719)
> コードベース全体(約16,400行)を2系統のエージェント調査(アプリ/UI層、ジオメトリ/永続化層)で精査し、
> Web・GitHub の類似プロジェクト調査と合わせてまとめた作業計画。
> 現状: テスト 255件 / 44ファイル全パス。`追加機能.md`(2026-06)のロードマップは第5階層まで概ね実装済み。

---

## Part 1. リファクタリング

### 優先度S — パフォーマンス・正しさに直結(工数小〜中で効果大)

| # | 内容 | 場所 | 影響度 | 工数 |
|---|------|------|:---:|:---:|
| S1 | **keydown リスナの毎レンダー再登録**。`fitViewToContent` が `project` に依存するため、エンティティを動かすたび・数字を1文字打つたびに `window.addEventListener` の解除/再登録が走る | `useCadViewportInteractions.ts:734` | 高 | 小 |
| S2 | **ドラッグ中のスナップ索引全再構築**。`SNAP_INDEX_CACHE` のキーが `Project` オブジェクト参照のため、頂点ドラッグ中は毎 pointermove で WeakMap がミスし R-tree を作り直す | `app/snapping.ts:14-65` | 高 | 中 |
| S3 | **カーソル座標をグローバル state に毎 pointermove 書き込み**。`ui.statusMessage` 経由で `StatusBar` が `project` 全体を購読しており、マウス移動だけで選択面積の再計算が走る | `useCadViewportInteractions.ts:341-343`, `StatusBar.tsx:10-22` | 高 | 中 |
| S4 | **ナイフ分割の面積検証が二重かつ矛盾**。内部はスケール相対許容、ストア側は絶対値 `1e-3` で再検証。大きい図形は正当な分割が弾かれ、小さい図形は検証が無意味になる。ストア側チェックを削除し一本化 | `knifeSplit.ts:305-321`, `geometryActions.ts:166-171` | 高 | 小 |
| S5 | **`React.memo` ゼロ・セレクタ層なし**。9コンポーネントが `project` 全体を購読。1頂点動かすと全 `PolygonShape`/`VertexHandles`/プロパティパネル全 input が再描画。`store/selectors.ts` 新設 + 図形コンポーネントの memo 化 | `CadViewport.tsx`, `PropertyPanel.tsx` ほか | 高 | 中 |

### 優先度A — 設計負債の解消(将来の機能追加コストを下げる)

| # | 内容 | 場所 | 影響度 | 工数 |
|---|------|------|:---:|:---:|
| A1 | **`useCadViewportInteractions.ts`(750行)の分割**。pan/zoom・作図状態機械・頂点ドラッグ・移動・数値HUD・キーボードの10責務が1フックに同居(ref 11個、ストア購読23個)。`usePanZoom` / `useEntityDragging` / `useDrawingTools` / `useDrawingKeyboard` へ分割し、純ロジックを React 非依存関数へ | `components/cad/` | 高 | 大 |
| A2 | **作図ツール分岐の Strategy 化**。rect/circle/ellipse/guide/knife の5ツールが down/move/up の3箇所に同型コードをコピペ(計15ブロック)。`ToolBehavior` インターフェースを `toolRegistry.ts` に登録する形へ。**新ツール追加(円弧改良・寸法線等)の前提工事** | `useCadViewportInteractions.ts:192-508` | 高 | 大 |
| A3 | **キーボード処理の一元化**。`App.tsx` と viewport フックが `window` の keydown を取り合い、`stopImmediatePropagation` + effect 登録順という暗黙依存で共存。単一ディスパッチャ(`useGlobalShortcuts` + mode 分岐)へ。モーダル判定も DOM クエリ(`domGuards.ts`)からストア参照へ | `App.tsx:137-233`, `useCadViewportInteractions.ts:652-727` | 高 | 中 |
| A4 | **ジオメトリエンジンの注入機構**。`GeometryEngine` インターフェースはあるが7ファイルが `defaultEngine` を直 import しており差し替え不能。レジストリ(`getEngine()`/`setEngine()`)導入。**Clipper2 移行(機能F2)・テストのモック化の前提工事** | `polygonClippingEngine.ts:118` ほか7ファイル | 高 | 中 |
| A5 | **許容誤差(epsilon)の統一**。`numeric.ts` にスケール相対ヘルパが整備済みなのに、`corner.ts`/`validation.ts`/`offset.ts`/`area.ts` 等に生の `1e-9`〜`1e-18` が10箇所以上散在。同じ量(パラメータ t)を別基準で判定する不整合もある。用途別の名前付き許容誤差へ集約 | `geometry/` 横断 | 高 | 中 |
| A6 | **エラー処理規約の統一**。結果型/null/空配列+握り潰し/boolean/warnings配列/例外の6通りが並存。`knifeSplit.ts` の判別可能結果型 `{ok, reason}` を標準に採用し、まず repair/offset の `catch {}` と `projectCodec` の一律 null を解消 | 横断 | 高 | 大(段階実施) |
| A7 | **バージョンマイグレーション機構**。`SUPPORTED_VERSIONS` は集合照合のみで変換関数がなく、未知バージョンは JSON 破損と同じ null で無言拒否。`MIGRATIONS` チェーン + 理由付き結果型へ。`APP_VERSION` と package.json の二重管理も解消 | `projectCodec.ts:20,237,268` | 高 | 中 |

### 優先度B — 重複排除・堅牢化(独立に少しずつ実施可能)

| # | 内容 | 場所 | 工数 |
|---|------|------|:---:|
| B1 | 「描画可能レイヤ解決」ロジックが実質5箇所に重複、フォールバック挙動にも差異。`resolveDrawingLayer()` に統一 | `entityActions.ts`, `primitiveActions.ts`, `historyActions.ts`, `layerActions.ts` | 小 |
| B2 | `chamferSelected`/`filletSelected` がほぼ完全重複。`simplify`/`align`/`distribute` も同パターン。`mutateSelectedPolygons()` ヘルパへ | `geometryActions.ts:223-476` | 小 |
| B3 | `addPolygonFromOuter`/`importPolygonGeometries` の重複(import 側だけ `validateEntity` 未呼出という挙動差もある) | `entityActions.ts:35-101` | 小 |
| B4 | 「blur/Enter で確定」入力が3コンポーネントで重複、props→state ミラーの useEffect 反パターン。`<CommitInput>` に統合 | `PropertyPanelSections.tsx`, `LayerOutlinerSections.tsx` | 小 |
| B5 | モーダルの Escape+フォーカス復帰が3箇所重複(Tab トラップは3つとも未実装)。`useModalDismiss()` へ抽出しトラップも実装 | `ShortcutModal` / `ManualModal` / `ProjectManagerModal` | 小 |
| B6 | 有限性チェック(`finitePoint` 等)が8箇所に重複定義。`numeric.ts` に `isFinitePoint/isFiniteRing` を追加 | `geometry/` 横断 | 小 |
| B7 | `svgImport.ts` のリング入れ子解析が O(n²·m)(面積を毎回再計算)。ソート済み性質の活用 + `BBoxSpatialIndex` で改善。楕円も手書きループでなく既存 `ellipseToRing` を再利用 | `svgImport.ts:306-342,419-437` | 中 |
| B8 | `offset.ts` の逐次 union が実質 O(n²)。トーナメント方式マージ + `arcSegments` 自動減衰へ | `offset.ts:47-100` | 中 |
| B9 | `validation.ts` の自己交差判定 O(n²) が undo/redo のたび全ポリゴンに走る。空間インデックスでブロードフェーズ化 | `validation.ts:12-49`, `historyActions.ts` | 中 |
| B10 | `deleteLocalProject` だけインデックス書込失敗を「削除失敗」として返す(save 側は best effort)。方針統一 | `localProjectStore.ts:284-300` | 小 |
| B11 | 命名の不一致: `GuideLineEntity` が polyline/arc も表す(→`LinearEntity` へ、永続化互換は A7 のマイグレーションで吸収)。`touchProjectSettings` は実態が updatedAt 更新のみ | `projectTypes.ts:74-92`, `store/helpers.ts:17` | 中 |
| B12 | デッドコード: `mapPolygonGeometries`(参照0)削除、`selectMany`(テスト専用)整理、`updateEntitiesGeometryTransient` 非公開化、未使用依存 `i18next-browser-languagedetector` 削除 | 各所 | 小 |
| B13 | `filletRing`(125行・7条件ガード)と `chamferRing` の骨格重複。`mapSelectedCorners()` 高階関数へ + ガード条件に理由コメント | `corner.ts:81-273` | 中 |
| B14 | `PropertyPanel` の early-return 5分岐で同一レイアウトを5回記述。`<PanelShell>` 共通化 | `PropertyPanel.tsx:54-209` | 中 |

### テスト補強(リファクタリングの安全網として先行)

- **`numeric.test.ts` 新設** — 補償加算の桁落ち、tolerance ヘルパのスケール不変性、`pointOnSegment` 境界。全演算の精度基盤なのにテストゼロ。(工数小)
- **i18n 整合テスト** — ja/en のキー集合一致検証(現在265キーで一致しているが CI で保証されていない)。(工数小)
- **`polygonClippingEngine` 直接テスト** — A4(エンジン差し替え)の受け入れ基準を兼ねる。(工数小)
- `geometry/snap.ts`, `translate.ts`, `persistence/projectFileIo.ts` の直接テスト追加。

---

## Part 2. 機能追加候補(Web / GitHub 調査に基づく)

### F1. DXF インポート ★推奨最優先
- **内容**: DXF(LWPOLYLINE / LINE / ARC / CIRCLE / POLYLINE)を読み込みポリゴン・線要素化する。エクスポートは実装済みで、対象ユーザー(建築・構造)にとって最も価値の高い未実装の相互運用機能。
- **調査結果**: [dxf-parser](https://github.com/gdsestimating/dxf-parser)(実績最多・ブラウザ対応)、[dxf-json](https://github.com/dotoritos-kim/dxf-json)(AutoCAD 2024 リファレンス準拠・TS)、[skymakerolof/dxf](https://github.com/skymakerolof/dxf)(SVG 変換内蔵)が候補。自前 DXF 書き出しが既にあるため、読み込み対象を書き出しと同じサブセット(AC1015 / LWPOLYLINE 中心)に絞れば依存を小さくできる。
- **難易度**: 中(サブセット限定なら) / **価値**: 高

### F2. ジオメトリエンジンの Clipper2 移行(または併用)
- **内容**: 現行 `polygon-clipping`(Martinez-Rueda, 更新停滞気味)を [Clipper2](https://github.com/AngusJohnson/Clipper2) の TS 移植 [clipper2-ts](https://github.com/countertype/clipper2-ts)(npm: `clipper2-ts`)へ差し替え可能にする。
- **効果**: ①堅牢で高速なブーリアン、②**ネイティブのオフセット API**(現行の「円+帯を大量 union」自前実装 B8 を丸ごと置換、マイター/ラウンド/スクエア結合選択可)、③開パス(ポリライン)のオフセット、④三角形分割。
- **前提**: リファクタ A4(エンジン注入機構)。両エンジンをテストで突き合わせてから切替できる。
- **難易度**: 中 / **価値**: 高

### F3. 寸法線・注釈エンティティ
- **内容**: 測定ツール(現在は一時表示のみ)の結果を図面要素として保存できる寸法線(線形寸法・角度寸法)とテキスト注釈。SVG/PNG/DXF 書き出しにも含める(DXF は DIMENSION でなく TEXT+線分の分解出力で可)。
- **根拠**: 測定基盤・スナップ・レイヤーは実装済みで、エンティティ型の追加が主作業。[OpenCAD](https://opencad.dev/) や [cad-viewer](https://github.com/mlightcad/cad-viewer) など Web CAD は寸法表示を標準装備しており、図面として共有する用途では事実上必須。
- **前提**: A2(ツール Strategy 化)、A7(マイグレーション — エンティティ型追加のため)。
- **難易度**: 中〜高 / **価値**: 高

### F4. 断面性能・面積レポート
- **内容**: 断面二次モーメント Ix/Iy、断面係数、回転半径を選択ポリゴン(穴対応)で算出し PropertyPanel に表示。加えて全ポリゴンの面積・周長・重心の一覧を印刷用レポート(`window.print()` + `@media print`)として出力。
- **根拠**: 建築・構造という対象ユーザーに直結。Green の定理による多角形の断面性能計算は既存 `area.ts`/`measure.ts` の延長で外部依存なしに実装可能。
- **難易度**: 低〜中 / **価値**: 中〜高(対象ユーザーには高)

### F5. GeoJSON インポート / エクスポート
- **内容**: GeoJSON Polygon/MultiPolygon の読み書き。データモデル(outer + holes)が GeoJSON のリング構造とほぼ1:1で対応するため変換は薄い。
- **根拠**: [geojson.io](https://geojson.io) や測量・敷地検討系ツールとの相互運用([awesome-geojson](https://github.com/tmcw/awesome-geojson) にエコシステム多数)。敷地・建物外形の面積検討というユースケースと親和性が高い。
- **難易度**: 低 / **価値**: 中

### F6. パラメトリック寸法拘束(長期テーマ)
- **内容**: 辺長・角度・平行・直交などの拘束を図形に付与し、寸法値の変更で形状が追従する。
- **調査結果**: [JSketcher](https://github.com/xibyte/jsketcher) が純 JS/TS の2D拘束ソルバを実装済み(MIT)。[assemble2d](https://github.com/tab58/assemble2d) はエネルギー最小化型の軽量ソルバ。ソルバ部分の移植・参考実装として利用可能。
- **注意**: データモデル・UI とも大改修になるため、A1/A2/A7 完了後の長期テーマとして位置づける。
- **難易度**: 高 / **価値**: 高(差別化機能)

### F7. その他の候補(小粒・随時)
| 機能 | 概要 | 難易度/価値 |
|------|------|------|
| 下絵画像トレース | 画像を半透明背景に配置しスケール合わせしてトレース。dataURL 肥大化対策に IndexedDB 保存 | 中 / 中〜高 |
| グループ化 | 複数エンティティの束ね。アウトライナーとの統合 | 中〜高 / 中 |
| ルーラー表示 | ビューポート端に目盛り。ガイドのドラッグ生成と統合可 | 中 / 中 |
| PWA 化(オフライン対応) | manifest + Service Worker。バックエンド不要の特性と好相性 | 低〜中 / 中 |
| テンプレート/部品ライブラリ | よく使う形状の保存・呼び出し。`localProjectStore` の基盤を共有 | 中 / 中 |
| LICENSE ファイル追加 | README に「LICENSE なし」と明記されている。配布方針の明確化 | 低 / 高(公開物として) |

---

## Part 3. 推奨ロードマップ

```
フェーズ0: 安全網            テスト補強(numeric / i18n / engine) + B12 デッドコード削除
フェーズ1: 即効性能改善      S1 → S4 → S3 → S2 → S5     … ユーザー体感に直結、各項目独立
フェーズ2: 基盤リファクタ    A4(エンジン注入) → A5(epsilon) → A7(マイグレーション) → A3(キーボード)
フェーズ3: 相互運用機能      F1(DXF import) → F5(GeoJSON) → F4(断面性能・レポート)
フェーズ4: 大型リファクタ    A1/A2(viewport フック分割 + ツール Strategy 化) → B系を随時
フェーズ5: 機能拡張          F2(Clipper2) → F3(寸法線・注釈) → F7 小粒随時
長期:                        F6(パラメトリック拘束)
```

**フェーズ順の理由**:
- フェーズ0/1 は独立性が高く即日効果。リファクタ前にテストの穴(numeric 等)を塞ぐ。
- A4(エンジン注入)は F2 の、A2(ツール Strategy)は F3 の、A7(マイグレーション)は F3/エンティティ型追加の**前提工事**。機能追加の直前に対応する依存リファクタを済ませる並び。
- F1(DXF import)はリファクタ依存が薄く、価値が最も高いので基盤工事と並行可能。

## Part 4. 進め方の規約(提案)

- 1項目 = 1ブランチ = 1PR。`typecheck` + `vitest` + `build` 通過を必須(既存 CI 踏襲)。
- S/A 項目は着手前に対象モジュールの回帰テストを追加してから改修(特に A5 epsilon 変更は挙動差が出やすい)。
- B11/A7 など永続化フォーマットに触る変更は、旧形式の読み込みテストをフィクスチャ JSON で固定。
- 本ファイルをタスク管理台帳とし、完了項目にチェック(`- [x]`)を付けて更新していく。

## 参考リンク(調査ソース)

- [Clipper2 本家 (C++/C#)](https://github.com/AngusJohnson/Clipper2) / [ドキュメント](https://www.angusj.com/clipper2/Docs/Overview.htm) / [clipper2-ts (TS移植)](https://github.com/countertype/clipper2-ts) / [npm](https://www.npmjs.com/package/clipper2-ts)
- [dxf-parser](https://github.com/gdsestimating/dxf-parser) / [dxf-json](https://github.com/dotoritos-kim/dxf-json) / [skymakerolof/dxf](https://github.com/skymakerolof/dxf)
- [awesome-cad (キュレーションリスト)](https://github.com/mlightcad/awesome-cad) / [cad-viewer (ブラウザ DXF/DWG エディタ)](https://github.com/mlightcad/cad-viewer) / [OpenCAD](https://opencad.dev/) / [Maker.js](https://github.com/microsoft/maker.js)
- [JSketcher (2D拘束ソルバ)](https://github.com/xibyte/jsketcher) / [assemble2d](https://github.com/tab58/assemble2d)
- [awesome-geojson](https://github.com/tmcw/awesome-geojson)

---

## Part 5. 実施結果 (2026-07-29)

ユーザー指示に従い、本計画に限って「1項目 = 1PR」規約を上書きし、
`codex/implement-work-plan` の単一ブランチ・単一PRへ統合した。
プロジェクト形式と package version は `0.3.0`。

### リファクタリング

- [x] S1 — keydown を単一 global shortcut dispatcher へ集約し、再レンダー時の再登録を解消。
- [x] S2 — `snapRevision` をキーにしたスナップ空間索引キャッシュへ変更。
- [x] S3 — pointermove 座標を外部 viewport status store へ分離。
- [x] S4 — ナイフ分割のストア側二重面積検証を削除。
- [x] S5 — selector 層を追加し、図形・頂点コンポーネントを memo 化。
- [x] A1 — viewport interaction を pan/zoom、entity drag、drawing、keyboard の各 hook へ分割。
- [x] A2 — `ToolBehaviorRegistry` と down/move/up Strategy を導入。
- [x] A3 — App、viewport、menu、modal のキーボード経路を一元化。
- [x] A4 — `getEngine` / `setEngine` / `resetEngine` と旧エンジン切替を実装。
- [x] A5 — 有限性検証と用途別 tolerance を `numeric.ts` へ集約。
- [x] A6 — repair / offset / project decode に理由付き結果型を導入。
- [x] A7 — 0.1→0.2→0.3 migration chain と package.json 単一バージョン源を実装。
- [x] B1〜B3 — 描画レイヤ解決、ポリゴン変更、追加・import 経路を共通化。
- [x] B4〜B5 — `CommitInput` と focus trap 付き `useModalDismiss` を共通化。
- [x] B6〜B9 — finite helper、ring nesting index、balanced union、自己交差 broad phase を実装。
- [x] B10 — ローカル削除後の補助索引更新を best effort に統一。
- [x] B11 — `LinearEntity` を正規名にし、旧 `GuideLineEntity` alias と保存互換を維持。
- [x] B12 — 未使用 helper・依存を削除し、transient geometry 更新をストア内部へ限定。
  `selectMany` はグループ／部品配置UIでも使用する正式操作へ昇格。
- [x] B13 — fillet / chamfer の選択角処理を共通 corner mapper へ集約。
- [x] B14 — `PanelShell` で PropertyPanel の共通レイアウトを統合。

### テスト安全網

- [x] 補償加算、相対 tolerance、有限性を含む `numeric.test.ts`。
- [x] ja/en キー集合一致テスト。
- [x] legacy / Clipper2 engine の直接・比較テスト。
- [x] snap、translate、project file I/O、migration、破損データ回復の直接テスト。
- [x] global shortcut、Strategy dispatch、focus trap、panel、ruler、PWA のUI回帰テスト。

### 機能追加

- [x] F1 — DXF の LWPOLYLINE / POLYLINE / LINE / ARC / CIRCLE 読込。
- [x] F2 — `clipper2-ts` を既定 engine とし、native offset と旧 engine rollback を実装。
- [x] F3 — 線形寸法・角度寸法・注記の作図、保存、表示、SVG / PNG / DXF 出力。
- [x] F4 — 穴対応の Ix / Iy、断面係数、回転半径と印刷用断面レポート。
- [x] F5 — GeoJSON Polygon / MultiPolygon の双方向変換。
- [x] F6 — 長さ、水平、垂直、角度、平行、直交の反復拘束 solver と操作UI。
- [x] F7 — IndexedDB 下絵、グループ、両軸ルーラー、PWA、部品テンプレート、
  `LICENSE` / `UNLICENSED` 方針を実装。

### 完了条件

- [x] `npm run typecheck`
- [x] `npm test -- --run`（62ファイル / 331テスト）
- [x] `npm run build`
- [x] `git diff --check`
